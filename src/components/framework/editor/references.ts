import serieUtils from '@/components/chart/serieUtils'

export const TOKENS = Object.keys(serieUtils).concat([
  'renderer',
  'bar',
  'options',
  'option',
  'source',
  'time',
  'indicatorId'
])
